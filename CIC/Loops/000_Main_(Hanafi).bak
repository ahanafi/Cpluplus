#include <iostream.h>
#include <stdio.h>
#include <conio.h>

//INCLUDED FILES
#include "001_FromTo_(Ardi).cpp";
#include "002_Modulus_(Gagan).cpp";
#include "003_Validation_(Hanafi).cpp";
#include "004_Arithmetics_(Hanafi).cpp";

main()
{
	printf("Hallo!");
	int pilihan;

   do{
	   printf("Masukkan pilihan Anda [1/2/3/4] : ");
   	scanf("%d", &pilihan);
   }while(pilihan >= 5|| pilihan <= 0);

   if(pilihan == 1){
   	NumberFromTo();
   } else if(pilihan == 2) {
   	Modulus();
   } else if(pilihan == 3) {
   	Validation();
   } else if(pilihan == 4) {
   	Aritmetics();
   } else {
   	clrscr();
      printf("\aMaaf pilihan anda salah!");
   }

   getch();

}